<p id="notice"></p>
    <div class="d-grid col-4 mx-auto">
        <h1>Editing Book</h1>


            <% if @book.errors.any? %>
              <%= @book.errors.count %> errors prohibited this obj from being saved:
                  <ul>
                    <% @book.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                    <% end %>
                  </ul>
            <% end %>


          <%= form_with model: @book  do |f| %>
             <p>title</p>
              <%= f.text_field :title %>
            <p>Opinion</p>
             <div class="opinion"> <%= f.text_area :body %></div>

              <br>  <%= f.submit 'Update Book', class: "btn btn-success"  %>
               <p id="notice"></p>
                   <br> <%= link_to 'Show', book_path %> | <%= link_to 'Back', books_path %>

          <% end %>


</div>